<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Product - Admin Panel</title>
    <link rel="stylesheet" href="/stylesheet/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="admin-container">
        <!-- Same sidebar as Admin.ejs -->
        <%- include('sidebar') %>

        <main class="main-content">
            <header class="content-header">
                <h1><i class="fas fa-plus"></i> Add New Product</h1>
                <a href="/admin" class="button secondary"><i class="fas fa-arrow-left"></i> Back to Products</a>
            </header>

            <div class="form-container">
                <form class="product-form" action="/admin/create" method="post" enctype="multipart/form-data">
                    <div class="form-group">
                        <label><i class="fas fa-tag"></i> Product Name</label>
                        <input type="text" placeholder="Enter product name" name="productName" required>
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-list"></i> Category</label>
                        
                        <select name="productCategory" required>
                            <option value="" disabled selected>Select a category</option>
                            <option value="electronics">Electronics</option>
                            <option value="feature">Feature</option>
                            <option value="homeandliving">HomeAndLiving</option>
                            <option value="sports">Sports</option>
                            <option value="fashion">Fashion</option>
                            <option value="slider">Slider</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-dollar-sign"></i> Price</label>
                        <input 
                            type="number" 
                            name="productPrice" 
                            step="0.01" 
                            min="0.01" 
                            required 
                            placeholder="Enter price"
                        >
                        <small class="error-message" style="color: red; display: none;">Price must be a positive number.</small>
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-boxes"></i> Stock Quantity</label>
                        <input type="number" name="productQuantity" required>
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-image"></i> Product Image</label>
                        <input type="file" name="productImage">
                    </div>
                    <div class="form-group">
                        <label><i class="fas fa-align-left"></i> Description</label>
                        <textarea rows="4" name="productDescription"></textarea>
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="button"><i class="fas fa-save"></i> Save Product</button>
                        <button type="button" class="button secondary" onclick="window.location.href='/admin';"><i class="fas fa-times"></i> Cancel</button>
                    </div>
                </form>
            </div>
        </main>
    </div>
</body>

</html>