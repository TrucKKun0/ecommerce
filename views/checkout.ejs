<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheet/checkout.css">
</head>
<body>
    <div class="container">
        <h1>Checkout</h1>
        
        <div class="checkout-container">
            <div class="checkout-form">
                <form method="post" action="/order/<%= user._id %>">
                    <div class="form-section">
                        <h2>Shipping Information</h2>
                        <div class="form-row">
                            <div class="form-group">
                                <label for="firstName">Username</label>
                                <textarea id="productPrice" name="username" placeholder="Enter product price"><%= user.username %></textarea>
                            </div>
                            <div class="form-group">
                                <label for="lastName">Email</label>
                                <textarea id="productPrice" name="email" placeholder="Enter product price"><%= user.email %></textarea>
                            </div>
                        </div>
                        
                        <div class="form-group">
                            <label for="address"> Address</label>
                            <input type="text" name="address" id="address" required>
                        </div>
                        <div class="form-row">
                            <div class="form-group">
                                <label for="city">City</label>
                                <input type="text" name="city" id="city" required>
                            </div>
                            <div class="form-group">
                                <label for="state">State</label>
                                <input type="text" name="state" id="state" required>
                            </div>
                            
                        </div>
                    </div>

                    <button type="submit" class="submit-btn">Place Order</button>
                </form>
            </div>

            <div class="order-summary">
                <h2>Order Summary</h2>
                <div class="order-items">
                    <% user.cart.forEach((item)=>{
                        %>
                        <div class="order-item">
                            <img src="/public/images/<%= item.productCategory %>/<%= item.productImage %>" alt="Backpack">
                            <div class="item-info">
                                <h3><%= item.productName %></h3>
                                
                                <p class="price">Rs <%=item.productPrice %></p>
                            </div>
                        </div>
                    <%})%>

                </div>
                <div class="summary-details">
                    <div class="summary-row">
                        <span>Subtotal</span>
                        <span>Rs<%=subtotal %></span>
                    </div>
                    <div class="summary-row">
                        <span>Shipping</span>
                        <span>Free</span>
                    </div>
                    <div class="summary-row">
                        <span>Platform Fee</span>
                        <span>Rs 100</span>
                    </div>
                    <div class="summary-row total">
                        <span>Total</span>
                        <span>Rs<%= totalPrice %></span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html> 