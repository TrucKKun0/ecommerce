<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    <link rel="stylesheet" href="/stylesheet/edit.css">
</head>
<body>
    <form class="product-form" action="/admin/update/<%= products._id %>" method="post" enctype="multipart/form-data">
        <div class="form-group">
            <label for="productName">Product Name</label>
            <textarea id="productName" name="productName" placeholder="Enter product name"><%= products.productName %></textarea>
        </div>
        <div class="form-group">
            <label for="productCategory">Category</label>
            <textarea id="productCategory" name="productCategory" placeholder="Enter product category"><%= products.productCategory %></textarea>
        </div>
        <div class="form-group">
            <label for="productPrice">Price</label>
            <textarea id="productPrice" name="productPrice" placeholder="Enter product price"><%= products.productPrice %></textarea>
        </div>
        <div class="form-group">
            <label for="productQuantity">Stock Quantity</label>
            <textarea id="productQuantity" name="productQuantity" placeholder="Enter stock quantity"><%= products.productQuantity %></textarea>
        </div>
        <div class="form-group">
            <label for="productQuantity">Update Image</label>
            <input type="file" name="productImage" >
        </div>
        <div class="form-group">
            <label for="productDescription">Description</label>
            <textarea id="productDescription" rows="4" name="productDescription" placeholder="Enter product description"><%= products.productDescription %></textarea>
        </div>
        <div class="form-actions">
            <button type="submit" class="save-btn">Update Product</button>
            <button type="button" class="cancel-btn" onclick="window.location.href='/admin/products';">Cancel</button>
        </div>
    </form>
</body>
</html>
