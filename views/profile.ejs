<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile - ShopEase</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="shortcut icon" href="images/Favicon/shopease.png" type="image/x-icon"/>

    
    <link rel="stylesheet" href="/stylesheet/profile.css">
</head>
<body>
    <div class="container">
        <h1><%=user.username %>'s Profile</h1>
        
        <div class="profile-section">
            <h2>Personal Information</h2>
            <p><strong>Name:</strong><%= user.username %> </p>
            <p><strong>Email:</strong> <%= user.email %> </p>
            
            <button onclick="alert('Edit functionality not implemented')">Edit Profile</button>
        </div>

        <div class="profile-section">
            <h2>Order History</h2>
            <% user.order.forEach(order => { %>
                <div class="order-history">
                    <p><strong>OrderId <%= order._id %></strong> - Date: <%= order.date%></p>
                   <% user.cart.forEach (item =>{%>
                    
                       <p>Total: Rs <%= item.totalPrice %></p>
                       <p>items: </p>
                   <%}) %>
                </div>
              <% }); %>
            
            
        </div>

        <a href="/" class="back-to-store">Back to Store</a>
    </div>

    <script>
        // You can add JavaScript here for any profile-related functionality
    </script>
</body>
</html>